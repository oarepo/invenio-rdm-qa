# ---------------------------------------------------------------
# Playwright TypeScript Test Automation Framework .gitignore
# ---------------------------------------------------------------

# Node.js dependencies and build artifacts
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*
.npm
.yarn-integrity
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*

# Compiled files (TypeScript output)
# Uncomment these if you do not commit generated JavaScript files to your repository.
# This is generally recommended for TypeScript projects where JS is a build artifact.
# *.js
# *.js.map
# *.d.ts

# TypeScript cache
*.tsbuildinfo

# Playwright test outputs and cache
playwright-report/
test-results/
test-results.json
blob-report/
playwright/.cache/

# Environment variable files (security sensitive)
# Crucial: Never commit your actual .env files with sensitive data.
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# IDE and Editor-specific files (Local / Temporary)
.history/             # VS Code local file history
.nrp/                 # Specific to some VS Code extensions
.vscode/              # VS Code project settings (uncomment to ignore if not shared team settings)
# .vscode/settings.json # Often ignored if specific to individual developer
# .vscode/launch.json   # Debug configurations, often ignored
# .vscode/extensions.json # Can be committed if used to recommend extensions for team

.idea/                # IntelliJ / WebStorm IDE files
*.sublime-project     # Sublime Text project files
*.sublime-workspace   # Sublime Text workspace files
*.DS_Store            # macOS specific files
Thumbs.db             # Windows specific files

# Other temporary and log files
logs
*.log
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json # Node.js diagnostic reports
pids
*.pid
*.seed
*.pid.lock
coverage              # Code coverage reports directory
*.lcov
.eslintcache          # ESLint cache
.stylelintcache       # Stylelint cache
.cache                # General cache directories
.node_repl_history    # Node.js REPL history
*.tgz                 # npm pack output (package archives)

# Python virtual environment (if accidentally created or present)
.venv/