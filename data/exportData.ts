/**
 * This file contains test data specific to the application's export functionality.
 * 
 * It defines preset values used when testing data export in various formats and citation styles.
 * 
 * Includes:
 * - Citation style (e.g., 'Harvard')
 * - Export format or option (e.g., 'JSON-LD')
 * 
 * Usage:
 * Import this module in tests that verify export functionality to easily manage and update export parameter values.
 */

export const exportData = {
  style: 'Harvard',
  exportOption: 'JSON-LD',
};